/**
IDEE

$Stylesheet = $Document.Stylesheet(); => create and get a new empty $Stylesheet;
$Stylesheet = $Document.Stylesheet($Element); => get a $Stylesheet from an existing <style> or <link rel='stylesheet'> element;
$Stylesheet = $Document.Stylesheet(csstext); => create and get a $Stylesheet from css text (prefixed);
$Stylesheet.remove() => delete whole stylesheet
$TextNode = $Stylesheet.set(csstext) => set whole stylesheet to csstext (prefixed);
$TextNode = $Stylesheet.add(csstext) => add csstext (prefixed) to stylesheet;
$Stylesheet.remove($TextNode) => remove Part from stylesheet;
$Stylesheet.replace($TextNode,csstext) => modify Part from stylesheet with csstext (prefixed);

IDEE on stereoids ;)

$Stylesheet.rule(...) = vein.js insert,modify or remove a rule (prefixed);
$Stylesheet.@rule(...) = vein.js insert,modify or remove a at-rule8 @media,@keyframe,etc (prefixed);
*/


